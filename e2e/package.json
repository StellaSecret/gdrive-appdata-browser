{
  "name": "appdata-browser-web-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Smoke-test e2e suite for the appDataFolder Browser static build.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
